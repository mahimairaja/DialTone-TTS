# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/

# Environments and tooling
.venv/
.env
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# Editors
.idea/
.vscode/
.DS_Store

# Benchmark run outputs. Regenerate with scripts/run_m0.sh; the numbers that are
# meant to be durable live in baselines.yaml and docs/, not here.
results/

# Internal working documents, not part of the public release
docs/specs/
docs/plans/

# Explainer video: a separate tool with its own environment, kept out of the
# published repo. See video/README.md for how to render it.
video/
media/
results.previous/
results.*/
samples/
